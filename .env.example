# Phase 3 — Backend API
NEXT_PUBLIC_API_URL=https://agents.korvuz.site/api

# Phase 3 — PostgreSQL
POSTGRES_PASSWORD=change_me

# Phase 3 — MinIO
MINIO_USER=admin
MINIO_PASSWORD=change_me

# Phase 5 — API Key Vault encryption
FERNET_MASTER_KEY=generate_with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
