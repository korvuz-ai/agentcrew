name: agent-company

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY}
        - NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
        - NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
        - NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/onboarding
        - NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-https://agents.korvuz.site/api}
    image: agent-company-frontend:latest
    container_name: agent-company-frontend
    ports:
      - "127.0.0.1:8790:3000"
    environment:
      NODE_ENV: production
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY}
    restart: unless-stopped

  # Phase 3 — uncomment when backend is ready
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: agentco
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #     POSTGRES_DB: agentco
  #   restart: unless-stopped
  #
  # redis:
  #   image: redis:7-alpine
  #   restart: unless-stopped
  #
  # minio:
  #   image: minio/minio
  #   command: server /data --console-address ":9001"
  #   environment:
  #     MINIO_ROOT_USER: ${MINIO_USER}
  #     MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD}
  #   restart: unless-stopped
